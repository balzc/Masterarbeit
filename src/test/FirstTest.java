package test;

public class FirstTest {
	/* train GP
	 * predict for following days
	 * set up mdp
	 * find best policy
	 * heat according to policy
	 * calculate utility
	 * repeat with gathered data
	 */
}
